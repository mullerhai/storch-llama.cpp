package torch.llama.enums

/**
 * The log output format (defaults to JSON for all server-based outputs).
 */
enum LogFormat {
  case JSON, TEXT
}
//object LogFormat extends Enumeration {
//  type LogFormat = Value
//  val JSON, TEXT = Value
//}